// Rimefall starts as a skeleton over NewOrigins and diverges one file at a time.
// Stage 3 replaces this shim with the band slot registry, the per-turn gateway rebuild and the
// SetupFaction refusal path; stages 4 to 6 add the starting alliances, both invasion fronts and
// the election. See docs/decisions/0010 and 0011.
#include "../neworigins/extra.cpp"
